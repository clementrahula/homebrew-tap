# scribe, in this project

<!-- covers: none -->

The role itself is shared: `~/.claude/agents/scribe.md`, from the workflow repository.
This file is the part that is only true here, and it wins where the two disagree.

Documents are `README.md` and `AGENTS.md` at the root. Cask definitions under `Casks/` are configuration, not documentation, and are not this role's to edit.

There is no project ownership map, so `~/.claude/doc-owners.json` is the whole of it.

This repository is **public** and its README carries the install command a stranger will copy. It is drafted rather than edited, and a wrong command there is worse than a wrong sentence.
